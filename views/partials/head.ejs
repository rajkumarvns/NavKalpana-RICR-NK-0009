<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ACIE Engine | NavKalpana</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Poppins', sans-serif; }
        .features-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; padding: 2rem 5%; }
        .f-card { background: white; padding: 2rem; border-radius: 20px; border: 1px solid #eef2ff; box-shadow: 0 4px 6px rgba(0,0,0,0.02); transition: 0.3s ease; }
        .f-card:hover { transform: translateY(-10px); border-color: #4f46e5; box-shadow: 0 20px 25px -5px rgba(79, 70, 229, 0.1); }
        .f-icon { font-size: 2.5rem; margin-bottom: 1rem; display: block; }
        .f-tag { font-size: 10px; text-transform: uppercase; background: #eef2ff; color: #4f46e5; padding: 4px 10px; border-radius: 50px; font-weight: 700; }
    </style>
</head>



